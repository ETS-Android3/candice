package com.example.candice.utility;

import androidx.fragment.app.Fragment;

//Listener for fragment changing
public interface FragmentChangeListener {
    void replaceFragment(Fragment fragment);
}
